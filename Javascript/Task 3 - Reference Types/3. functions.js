// performaing a task
function greet(name, regards) {
    
    console.log('Hello ' + name + ' ' + regards);
}

// Calculating a value
function square(number) {
    return number * number;
}

console.log(square (2))

greet('John,','we hope you enjoy your stay!');
greet('Jane,','we hope you have a great day!');